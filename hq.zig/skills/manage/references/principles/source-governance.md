# Source Governance

Status: existing

## Meaning

Use approved sources only.
Keep `spec`, `oc`, and `gpt` role boundaries explicit.
Prefer one-shot access over repeated checking.

## Existing Reflection

- `SKILL.md` requires approved sources only
- `RUNBOOK.md` and `recovery.md` make polling exceptional
- `SKILL.md` defines the `spec -> oc -> gpt` structure and the reporting boundary

## Completion Target

This principle is already strong and should remain the baseline for all managed runs.

## Next Entry Point

- Read `evidence-separation.md`.
