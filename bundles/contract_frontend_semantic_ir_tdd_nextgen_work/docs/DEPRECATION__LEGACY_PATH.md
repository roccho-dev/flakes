# Legacy path freeze

The original `rules/contract_frontend_semantic_ir.nemo` and `tests/manifest_{red,green}.tsv`
remain in the repository for parity, review, and historical comparison.

They are no longer the place to add new semantic responsibilities.
New semantic/provenance/test-spec/runtime work must land in `rules/nextgen/` and `tests_next/`.
